model PageView {
    id Int @id @default(autoincrement())
    path String
    viewdAt DateTime @default(now()) @map("viewed_at")
    ipAddress String?
    userAgent String?
    referrer String?

    @@index([path, viewdAt])
    @@map("page_views")
}